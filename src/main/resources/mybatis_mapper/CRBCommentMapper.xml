<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.pink_jelly.mapper.CRBCommentMapper">
    <insert id="insert" parameterType="com.example.pink_jelly.domain.CRBCommentVO" useGeneratedKeys="true" keyProperty="crbNo">
        INSERT INTO catscomment (crbNo, memberId, nickName, comment, parentNo, addDate, mno, profileImg)
        VALUES (#{crbNo}, #{memberId}, #{nickName}, #{comment}, #{parentNo}, now(), #{mno}, #{profileImg})
    </insert>
    <select id="selectAll" resultType="com.example.pink_jelly.domain.CRBCommentVO">
            SELECT * FROM catsComment WHERE crbNo=#{crbNo} ORDER BY comNo ASC
    </select>
    <select id="getCount" resultType="int">
        SELECT COUNT(comNo) FROM catsComment WHERE crbNo=#{crbNo}
    </select>
    <select id="selectList" resultType="com.example.pink_jelly.domain.CRBCommentVO">
        SELECT * FROM catsComment WHERE crbNo=#{crbNo} ORDER BY comNo LIMIT #{skip}, #{size}
    </select>
    <delete id="deleteOne">
        DELETE FROM catsComment WHERE comNo=#{comNo}
    </delete>
</mapper>